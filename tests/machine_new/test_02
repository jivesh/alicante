P = parse_and_compile(`
function f(x) {
  const y = x + 1;
  const z = y + 2;
  return (10 > y) ? z - 3 : z + 2;
}
f(7);
`);

initialize_machine(160);
run();

// expected: 
Running VM with heap size: 160
result: heap node of type = number, value = 7 ""
undefined
